% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htest-ttest-defs-formula.R
\name{ttest-formula}
\alias{ttest-formula}
\title{T-Test: Formula interface}
\description{
The formula implementation performs one-sample or two-sample t-tests
specified via a standard R formula. The response variable is taken from
the left-hand side; the right-hand side determines the test type:
\enumerate{
\item \code{y ~ group}: two-sample t-test, one test per grouping variable.
\item \code{y ~ 1}: one-sample t-test against \code{.mu}.
\item \code{y ~ group + 1}" both tests in a single call.
}

Use a formula directly as the model ID to select this implementation.
}
\section{Arguments}{

The following arguments are passed via \code{...} in \code{\link[=TTEST]{TTEST()}}:

\describe{
\item{\code{.mu}}{Numeric. Hypothesized mean or mean difference. Default \code{0}.}
\item{\code{.alt}}{String. One of \code{"two.sided"}, \code{"greater"}, or \code{"less"}.
Default \code{"two.sided"}.}
\item{\code{.ci}}{Numeric. Confidence level. Default \code{0.95}.}
}
}

\section{Variants}{

No variants are currently registered for the formula path. Use
\code{\link[=add_variant]{add_variant()}} to register custom variants at the user or package level.
}

\section{Result class}{

Returns a tibble with columns \code{type}, \code{group}, and \code{ttest} (a list-column
of \code{\link[stats:t.test]{stats::t.test()}} results). This path does not currently return a
\link{class_stat_infer} subclass — use \code{\link[=making_tidy]{making_tidy()}} to register a tidy
method if needed.
}

\examples{
sleep |>
    define_model(extra ~ group) |>
    prepare_test(TTEST) |>
    conclude()

# one-sample
sleep |>
    define_model(extra ~ 1) |>
    prepare_test(TTEST) |>
    conclude()

# both in one call
sleep |>
    define_model(extra ~ group + 1) |>
    prepare_test(TTEST) |>
    conclude()

}
\seealso{
Other ttest-implementations: 
\code{\link{ttest-pairwise}},
\code{\link{ttest-xby}}
}
\concept{ttest-implementations}
\keyword{internal}
