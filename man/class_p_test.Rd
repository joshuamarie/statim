% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htest-proptest.R
\name{class_p_test}
\alias{class_p_test}
\title{Structured result container for proportion tests}
\description{
An S7 class produced by \link{P_TEST} pipelines using \code{\link[=prop]{prop()}} as the model
ID. Not constructed manually — use the pipeline instead.

Inherits from \link{class_stat_infer}, so \code{\link[=auto_tidy]{auto_tidy()}} dispatches on it
automatically. Downstream packages can use it as a \code{parent} in
\code{S7::new_class()}.
}
\details{
Slots (populated automatically by \link{P_TEST}):
\itemize{
\item \code{x}: number of successes (input).
\item \code{n}: number of trials (input).
\item \code{estimate}: observed proportion (\code{x / n}).
\item \code{statistic}: test statistic. The count \code{x} for the default binomial;
the chi-squared value for \code{"prop"}.
\item \code{p_val}: p-value.
\item \code{lower_ci}: lower confidence bound.
\item \code{upper_ci}: upper confidence bound.
\item \code{ci_level}: confidence level, e.g. \code{0.95}.
}
}
\section{Shared by variants}{

Both \code{default} and \code{prop} return a \code{class_p_test}, so \code{\link[=auto_tidy]{auto_tidy()}} and
\code{\link[=print]{print()}} are inherited by \code{prop} for free.
}

\seealso{
\link{P_TEST}, \code{\link[=auto_tidy]{auto_tidy()}}, \link{class_stat_infer}
}
