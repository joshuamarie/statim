% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htest-ttest-defs-pairwise.R
\name{class_ttest_pairwise}
\alias{class_ttest_pairwise}
\title{Structured result container for pairwise t-tests}
\description{
An S7 class produced by \link{TTEST} pipelines using \code{\link[=pairwise]{pairwise()}} as the
model ID. Not constructed manually — use the pipeline instead.

Inherits from \link{class_stat_infer}, so \code{\link[=auto_tidy]{auto_tidy()}} dispatches on it
automatically. Downstream packages can use it as a \code{parent} in
\code{S7::new_class()}.
}
\details{
Slots (populated automatically by \link{TTEST}):
\itemize{
\item \code{var1}: first variable in each pair.
\item \code{var2}: second variable in each pair.
\item \code{est}: mean difference per pair (or sample mean for one-sample mode).
\item \code{df}: degrees of freedom per pair.
\item \code{t_stat}: t-statistic per pair.
\item \code{p_value}: p-value per pair.
\item \code{method_name}: scalar string describing the test method, taken directly
from \code{\link[stats:t.test]{stats::t.test()}}. Must be length 1 — all pairs must share the
same method.
}
}
\section{One-sample mode}{

When \code{\link[=pairwise]{pairwise()}} uses \code{direction = "eq"}, \code{var1} and \code{var2} are
identical (each variable tested against itself). \code{\link[=print]{print()}} detects this
and renders a diagonal-only matrix.
}

\seealso{
\link{TTEST}, \link{ttest-pairwise}, \code{\link[=auto_tidy]{auto_tidy()}}, \link{class_stat_infer}
}
