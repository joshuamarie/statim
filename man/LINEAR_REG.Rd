% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-linear-reg.R
\name{LINEAR_REG}
\alias{LINEAR_REG}
\title{Linear regression}
\usage{
LINEAR_REG(.model = NULL, .data = NULL, ...)
}
\arguments{
\item{.model}{A model ID from \code{\link[=define_model]{define_model()}}, or \code{NULL} to return a
\code{model_spec} for use in \code{\link[=prepare_model]{prepare_model()}}.}

\item{.data}{A data frame. Used when \code{.model} is supplied directly.}

\item{...}{Currently unused.}
}
\value{
A \code{cld_exec} object containing a \link{class_lm_object}, or a \code{model_spec}
when \code{.model = NULL}.
}
\description{
Fits an ordinary least squares linear regression model.
Accepts \code{\link[=rel]{rel()}} or a formula as the model ID.
}
\details{
The result is an \link{class_lm_object}, which satisfies the \code{\link[=anova]{anova()}}
protocol and prints coefficients and model fit universally across
all engines and variants.
}
\examples{
# via rel()
cars |>
    define_model(rel(speed, dist)) |>
    prepare_model(LINEAR_REG) |>
    conclude()

# via formula
cars |>
    define_model(dist ~ speed) |>
    prepare_model(LINEAR_REG) |>
    conclude()

# write_models() pipeline
LifeCycleSavings |>
    write_models(
        f1 = sr ~ 1,
        f2 = sr ~ pop15,
        f3 = sr ~ pop15 + pop75,
        f4 = sr ~ pop15 + pop75 + dpi,
        f5 = sr ~ pop15 + pop75 + dpi + ddpi
    ) |>
    prepare_model(LINEAR_REG) |>
    anova()

# individual conclude(), compare after
mod1 = LifeCycleSavings |> define_model(sr ~ 1) |> prepare_model(LINEAR_REG) |> conclude()
mod2 = LifeCycleSavings |> define_model(sr ~ pop15) |> prepare_model(LINEAR_REG) |> conclude()

anova(mod1, mod2)

}
