% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htest-proptest.R
\name{P_TEST}
\alias{P_TEST}
\title{Proportion Test}
\usage{
P_TEST(.model = NULL, .data = NULL, ...)
}
\arguments{
\item{.model}{A registered model ID, e.g. \code{\link[=prop]{prop()}}. When supplied, the test executes
immediately.}

\item{.data}{Unused. Accepted for pipeline consistency.}

\item{...}{Additional arguments passed to the implementation. See the
\strong{Arguments} and \strong{Variants} sections below.}
}
\value{
A \code{cld_exec} object, or a \code{test_spec} when \code{.model = NULL}.
The object stored in \code{cld_exec@data} is a \link{class_p_test} object.
}
\description{
\code{P_TEST()} performs a one-sample proportion test using either an exact
binomial test or a normal approximation. If \code{P_TEST} is supplied within the lazy-loaded pipeline,
supply \code{P_TEST} as a function within i.e. \code{prepare_test(.test = P_TEST)} call.
}
\section{Arguments}{

The following arguments are passed via \code{...} in \code{\link[=P_TEST]{P_TEST()}} or \code{\link[=via]{via()}}:

\describe{
\item{\code{.p}}{Numeric. Hypothesized proportion under H\eqn{_0}. Default \code{0.5}.}
\item{\code{.alt}}{Direction: \code{"two.sided"}, \code{"greater"}, or \code{"less"}.
Default \code{"two.sided"}.}
\item{\code{.ci}}{Confidence level. Default \code{0.95}.}
}
}

\section{Variants}{

\describe{
\item{\code{"prop"}}{Normal approximation via \code{\link[stats:prop.test]{stats::prop.test()}} without
continuity correction. Accepts the same \code{.p}, \code{.alt}, \code{.ci} arguments
as the default, except with \code{correct} addition to indicate whether Yates'
continuity correction should be applied or not.}
}
}

\section{Hypothesis claims}{

Supports \code{\link[=PI]{PI()}} via \code{\link[=state_null]{state_null()}}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{define_model(prop(45, 100)) |>
    prepare_test(P_TEST) |>
    state_null(PI() == 0.5) |>
    conclude()
}\if{html}{\out{</div>}}
}

\examples{
P_TEST(prop(45, 100))

# piped syntax
define_model(prop(45, 100)) |>
    prepare_test(P_TEST) |>
    conclude()

# normal approximation
define_model(prop(45, 100)) |>
    prepare_test(P_TEST) |>
    via("prop") |>
    conclude()

# hypothesis claim
define_model(prop(45, 100)) |>
    prepare_test(P_TEST) |>
    state_null(PI() == 0.3) |>
    conclude()

}
\seealso{
\code{\link[=prop]{prop()}}, \link{class_p_test}, \code{\link[=PI]{PI()}}, \code{\link[=state_null]{state_null()}}, \code{\link[=via]{via()}},
\code{\link[=conclude]{conclude()}}
}
