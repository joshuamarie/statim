% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htest-ttest-defs-pairwise.R
\name{ttest-pairwise}
\alias{ttest-pairwise}
\title{T-Test: Pairwise (\code{pairwise})}
\description{
The \code{pairwise} implementation performs pairwise t-tests across a set of
numeric variables. Each pair of variables is compared independently, and
results are presented as a matrix.

Use \code{\link[=pairwise]{pairwise()}} as the model ID to select this implementation.
}
\section{Arguments}{

The following arguments are passed via \code{...} in \code{\link[=TTEST]{TTEST()}}:

\describe{
\item{\code{.paired}}{Logical. Whether to perform paired comparisons.
Default \code{FALSE}.}
\item{\code{.mu}}{Numeric. Hypothesized mean or mean difference. Length 1
(applied to all pairs) or one value per variable. Default \code{0}.}
\item{\code{.alt}}{String. One of \code{"two.sided"}, \code{"greater"}, or \code{"less"}.
Default \code{"two.sided"}.}
\item{\code{.ci}}{Numeric. Confidence level. Default \code{0.95}.}
}
}

\section{Variants}{

No variants are currently registered for the \code{pairwise} path. Use
\code{\link[=add_variant]{add_variant()}} to register custom variants at the user or package level.
}

\section{Result class}{

Returns a \link{class_ttest_pairwise} object inheriting from \link{class_stat_infer}.
Results are printed as a pairwise matrix via \code{\link[tabstats:pairwise_matrix]{tabstats::pairwise_matrix()}}.
}

\section{One-sample mode}{

When \code{\link[=pairwise]{pairwise()}} is constructed with a \code{direction = "eq"} argument, each
variable is tested against its own \code{.mu} value rather than against another
variable. The result matrix displays diagonal entries only.
}

\examples{
iris |>
    define_model(pairwise(Sepal.Length, Sepal.Width, Petal.Length)) |>
    prepare_test(TTEST) |>
    conclude()

}
\seealso{
Other ttest-implementations: 
\code{\link{ttest-formula}},
\code{\link{ttest-xby}}
}
\concept{ttest-implementations}
\keyword{internal}
