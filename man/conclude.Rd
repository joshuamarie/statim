% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat-exec.R
\name{conclude}
\alias{conclude}
\title{Execute a lazy pipeline}
\usage{
conclude(.x, ...)
}
\arguments{
\item{.x}{A \code{test_lazy} or \code{model_lazy} object produced by
\code{\link[=prepare_test]{prepare_test()}} or \code{\link[=prepare_model]{prepare_model()}} (optionally followed by \code{\link[=via]{via()}}).}

\item{...}{Currently unused.}
}
\value{
A \code{cld_exec} S3 object.
}
\description{
\code{conclude()} is the terminal step of the pipeline. It resolves the
method variant, runs the implementation, and returns a \code{cld_exec} object.
}
\examples{
sleep |>
    define_model(x_by(extra, group)) |>
    prepare_test(TTEST) |>
    conclude()

sleep |>
    define_model(x_by(extra, group)) |>
    prepare_test(TTEST) |>
    via("boot", n = 2000) |>
    conclude()

mtcars |>
    define_model(rel(mpg, wt)) |>
    prepare_model(LINEAR_REG) |>
    conclude()

}
\seealso{
\code{\link[=prepare_test]{prepare_test()}}, \code{\link[=prepare_model]{prepare_model()}}, \code{\link[=via]{via()}}
}
