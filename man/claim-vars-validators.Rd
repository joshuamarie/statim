% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypothesis-validators.R
\name{claim-vars-validators}
\alias{claim-vars-validators}
\alias{validate_claim_vars}
\alias{check_param_nodes}
\alias{validate_one_param_node}
\alias{check_x_and_given}
\title{Validate hypothesis parameter references against a model's declared variables}
\usage{
validate_claim_vars(model_id, ...)

check_param_nodes(claims, x_vars, by_vars)

validate_one_param_node(node, ...)

check_x_and_given(x_quo, given_quo, x_vars, by_vars, cls_name)
}
\arguments{
\item{model_id}{A <\link{model_id}> object (usually carried by \code{\link[=define_model]{define_model()}}).}

\item{claims}{A single \code{null_claim} or a \code{null_claims} object.}

\item{x_vars}{A character vector of declared x-variable names, or \code{NULL}
to skip x-variable validation.}

\item{by_vars}{A character vector of declared grouping variable names, or
\code{NULL} to skip grouping variable validation.}

\item{node}{A \code{param_obj} instance.}

\item{x_quo}{A quosure holding the \code{x} slot value, or \code{NULL}.}

\item{given_quo}{A quosure holding the \code{given} slot value, or \code{NULL}.}

\item{cls_name}{A string naming the param class, used in error messages.}

\item{processed}{The processed list from \code{lazy@processed}, as returned by
\code{\link[=model_processor]{model_processor()}}.}
}
\value{
\code{invisible(NULL)}, or aborts with a consolidated error.
}
\description{
An S7 generic dispatched on the model ID class. Called automatically inside
\code{\link[=state_null]{state_null()}} after the compatible-param guard. Implement a method for any
new \link{model_id} subclass you define.
}
\details{
\code{check_param_nodes()} is the shared walker used by all built-in
\code{validate_claim_vars()} methods. It collects every \code{param_obj} node,
runs \code{validate_one_param_node()} on each, deduplicates errors, and aborts
with a consolidated message. Call this inside your own
\code{validate_claim_vars()} method rather than re-implementing the walking and
accumulation logic.

\code{validate_one_param_node()} is an S7 generic dispatched on \code{param_obj}.
Returns a character vector of error strings — empty if valid. The default
method on \code{param_obj} returns \code{character(0)}, so unknown subclasses pass
through safely. Implement a method for any new \code{param_obj} subclass whose
slots should be checked against the model's declared variables.

\code{check_x_and_given()} is the standard building block for
\code{validate_one_param_node()} methods on \code{param_obj} subclasses that follow
the \code{MU(x, given)} slot convention. It checks an \code{x} quosure against
\code{x_vars} and a \code{given} quosure against \code{by_vars}.
}
\section{Implementing a new method}{

By default, unknown model ID subclasses pass through without validation.
To add validation for a new \link{model_id} subclass, implement a method and
delegate to \code{check_param_nodes()}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{S7::method(validate_claim_vars, <model_id>) = function(model_id, processed, claims) \{
    check_param_nodes(
        claims,
        x_vars = names(processed$x_data),
        by_vars = names(processed$group_data)
    )
\}
}\if{html}{\out{</div>}}
}

\section{Implementing \code{validate_one_param_node} for a new param class}{

For a subclass with \code{x} and \code{given} slots, delegate to
\code{\link[=check_x_and_given]{check_x_and_given()}}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{S7::method(validate_one_param_node, MY_PARAM) = function(node, x_vars, by_vars) \{
    check_x_and_given(node@x, node@given, x_vars, by_vars, "MY_PARAM")
\}
}\if{html}{\out{</div>}}

For a subclass with two variable slots (like \code{\link[=RHO]{RHO()}}):

\if{html}{\out{<div class="sourceCode r">}}\preformatted{S7::method(validate_one_param_node, MY_PARAM) = function(node, x_vars, by_vars) \{
    errors = character(0)
    for (slot_quo in list(node@x, node@y)) \{
        lbl = rlang::as_label(slot_quo)
        if (!is.null(x_vars) && !lbl \%in\% x_vars) \{
            errors = c(errors, cli::format_inline(
                "Unknown variable \{.val \{lbl\}\} in \{.cls MY_PARAM\}. ",
                "Model declares x-variable\{?s\}: \{.and \{.val \{x_vars\}\}\}."
            ))
        \}
    \}
    errors
\}
}\if{html}{\out{</div>}}
}

\seealso{
\code{\link[=state_null]{state_null()}}, \code{\link[=MU]{MU()}}, \code{\link[=RHO]{RHO()}}, \code{\link[=PI]{PI()}}, \code{\link[=param_obj]{param_obj()}}, \code{\link[=model_id]{model_id()}}
}
