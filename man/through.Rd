% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htest-engine-set.R
\name{through}
\alias{through}
\alias{through.test_lazy}
\title{Set the computational engine for a test pipeline}
\usage{
through(.x, engine, ...)

\method{through}{test_lazy}(.x, engine, ...)
}
\arguments{
\item{.x}{A \code{test_lazy} object.}

\item{engine}{A string naming the engine. E.g. \code{"cpp"}, \code{"rust"}.}

\item{...}{Additional engine-level arguments.}
}
\value{
A \code{test_lazy} object.
}
\description{
\code{through()} sets the engine without changing the method variant.
The engine is inherited by \code{\link[=via]{via()}} if called afterwards.
}
\examples{
# The "cpp" engine is hypothetical; replace with a registered engine name.
\dontrun{
sleep |>
    define_model(x_by(extra, group)) |>
    prepare_test(TTEST) |>
    through("cpp") |>
    conclude()

sleep |>
    define_model(x_by(extra, group)) |>
    prepare_test(TTEST) |>
    through("cpp") |>
    via("boot", n = 2000) |>
    conclude()
}
}
\seealso{
\code{\link[=via]{via()}}, \code{\link[=conclude]{conclude()}}, \code{\link[=update]{update()}}
}
