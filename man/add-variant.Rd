% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-option.R
\name{add-variant}
\alias{add-variant}
\alias{add_variant}
\alias{remove_variant}
\title{Add or remove variant implementations on a test or model function}
\usage{
add_variant(obj, model_type, name, origin = c("user", "package"))

remove_variant(obj, model_type, name)
}
\arguments{
\item{obj}{A test or model function built with \code{\link[=HTEST_FN]{HTEST_FN()}} or \code{\link[=MODEL_FN]{MODEL_FN()}}
(e.g. \code{TTEST}). Used to scope the registry key.}

\item{model_type}{An S7 model ID class (e.g. \code{x_by}, \code{S7::class_formula}).}

\item{name}{A string naming the variant to add.}

\item{origin}{One of \code{"user"} (default, session-scoped) or \code{"package"}
(load-scoped, intended for \code{.onLoad()}).}
}
\value{
An \code{add_variant_call} object, consumed by \verb{\%<-\%}.
}
\description{
These are \strong{developer-interface} functions intended for package authors
extending the \code{statim} framework with new method variants.

\code{add_variant()} is used as the left-hand side of the \verb{\%<-\%} operator
to register a \code{\link[=variant]{variant()}} for a stat function and model type.
\code{"default"} is frozen and cannot be added.

\code{remove_variant()} removes a previously registered \code{"user"}-originated
variant. \code{"package"}-level entries are self-cleaning: they exist for the
duration the registering package is loaded and vanish when it is unloaded.
}
\examples{
# Add a bootstrap variant for x_by (user level).
# .proc$x_data[[1]] is the response vector; .proc$group_data is the
# grouping data frame. See ?model_processor for all available keys.
add_variant(TTEST, x_by, "another_boot") \%<-\% variant(
    fn = function(.proc, .n = 1000L) {
        x = .proc$x_data[[1]]
        grp = as.character(.proc$group_data[[1]])
        lvls = unique(grp)
        x1 = x[grp == lvls[[1]]]
        x2 = x[grp == lvls[[2]]]
        boot_fn = function(d, i) mean(d[i, 1]) - mean(d[i, 2])
        b = boot::boot(data.frame(x1, x2), boot_fn, R = .n)
        boot::boot.ci(b, type = "perc")
    }
)

# Remove it, returning to the original slate
remove_variant(TTEST, x_by, "another_boot")

# Package level (inside .onLoad())
add_variant(TTEST, x_by, "another_boot", origin = "package") \%<-\% variant(
    fn = function(.proc, .n = 1000L) { ... }
)

}
\seealso{
\code{\link[=stat_define]{stat_define()}}, \code{\link[=variant]{variant()}}, \code{\link[=agendas]{agendas()}}, \code{\link[=model_processor]{model_processor()}}
}
