% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htest-ttest.R
\name{class_ttest_two}
\alias{class_ttest_two}
\title{Structured result container for two-sample t-tests}
\description{
An S7 class produced by \link{TTEST} pipelines using \code{\link[=x_by]{x_by()}} as the model ID.
Not constructed manually — use the pipeline instead.

Inherits from \link{class_stat_infer}, so \code{\link[=auto_tidy]{auto_tidy()}} dispatches on it
automatically. Downstream packages can use it as a \code{parent} in
\code{S7::new_class()}.
}
\details{
Slots (populated automatically by \link{TTEST}):
\itemize{
\item \code{group}: name of the grouping variable.
\item \code{estimate}: mean difference (or linear contrast estimate).
\item \code{t_stat}: t-statistic.
\item \code{df}: degrees of freedom.
\item \code{p_val}: p-value.
\item \code{lower_ci}: lower confidence bound.
\item \code{upper_ci}: upper confidence bound.
\item \code{ci_level}: confidence level, e.g. \code{0.95}.
}
}
\section{Shared by variants}{

Both the default (\code{base}) and \code{contrast} return a \code{class_ttest_two}, so
\code{\link[=auto_tidy]{auto_tidy()}} and \code{\link[=print]{print()}} are inherited by \code{contrast} for free.
}

\seealso{
\link{TTEST}, \code{\link[=auto_tidy]{auto_tidy()}}, \link{class_stat_infer}
}
