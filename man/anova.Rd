% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htest-anova.R
\name{ANOVA}
\alias{ANOVA}
\title{ANOVA}
\usage{
ANOVA(.model = NULL, .data = NULL, ...)
}
\arguments{
\item{.model}{A model ID from \code{x_by()} or a formula.
When supplied, the test executes immediately.}

\item{.data}{A data frame. Only used on the standalone path.}

\item{...}{Additional arguments passed to the implementation.}
}
\value{
A \code{cld_exec} object (pipeline), or a \code{test_spec} object.
}
\description{
\code{ANOVA()} performs an analysis of variance for one-way, two-way,
or formula-based comparisons. If \code{ANOVA} is supplied within the lazy-loaded pipeline,
supply \code{ANOVA} as a function within i.e. \code{prepare_test(.test = ANOVA)} call.
}
\section{Supported model IDs}{

\itemize{
\item \code{x_by()}: one-way or two-way ANOVA
\item formula: standard \code{aov()} formula interface
}
}

\examples{
# pipeline — one-way
npk |>
    define_model(x_by(yield, block)) |>
    prepare_test(ANOVA) |>
    conclude()

# pipeline — two-way
npk |>
    define_model(x_by(yield, c(block, N))) |>
    prepare_test(ANOVA) |>
    conclude()

# formula interface
npk |>
    define_model(yield ~ block + N) |>
    prepare_test(ANOVA) |>
    conclude()

# with hypothesis
npk |>
    define_model(x_by(yield, block)) |>
    prepare_test(ANOVA) |>
    state_null(
        MU(yield, block == "1") \%=\%
        MU(yield, block == "2") \%=\%
        MU(yield, block == "3")
    ) |>
    conclude()

}
