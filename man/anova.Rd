% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-anova-mod.R
\docType{data}
\name{anova}
\alias{anova}
\title{ANOVA table for linear model comparisons}
\format{
An object of class \code{S7_external_generic} of length 4.
}
\usage{
anova
}
\arguments{
\item{object}{An \code{anova_lazy}, \code{model_lazy}, or \code{cld_exec} object.}

\item{...}{Additional \code{model_lazy} or \code{cld_exec} objects.}

\item{test}{A string. One of \code{"F"} (default), \code{"LRT"}, or \code{"Chisq"}.}
}
\value{
A \code{cld_anova} object, invisibly.
}
\description{
\code{anova()} computes an incremental F-test across two or more fitted linear
models. It dispatches on three input types:
}
\details{
\itemize{
\item An \code{anova_lazy} from \code{write_models() |> prepare_model()}.
\item One or more \code{model_lazy} objects from \code{prepare_model()}.
\item One or more \code{cld_exec} objects from \code{conclude()}.
}
}
\examples{
# via write_models()
LifeCycleSavings |>
    write_models(
        f1 = sr ~ 1,
        f2 = sr ~ pop15,
        f3 = sr ~ pop15 + pop75,
        f4 = sr ~ pop15 + pop75 + dpi,
        f5 = sr ~ pop15 + pop75 + dpi + ddpi
    ) |>
    prepare_model(LINEAR_REG) |>
    anova()

# via model_lazy
mod1 = LifeCycleSavings |> define_model(sr ~ 1) |> prepare_model(LINEAR_REG)
mod2 = LifeCycleSavings |> define_model(sr ~ pop15) |> prepare_model(LINEAR_REG)

anova(mod1, mod2)

# via conclude()
mod1 = LifeCycleSavings |> define_model(sr ~ 1) |> prepare_model(LINEAR_REG) |> conclude()
mod2 = LifeCycleSavings |> define_model(sr ~ pop15) |> prepare_model(LINEAR_REG) |> conclude()

anova(mod1, mod2)
anova(mod1, mod2, test = "LRT")

}
\seealso{
\code{\link[=write_models]{write_models()}}, \code{\link[=prepare_model]{prepare_model()}}, \code{\link[=conclude]{conclude()}}
}
\keyword{datasets}
