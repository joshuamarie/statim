% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htest-baseline-variant.R
\name{baseline}
\alias{baseline}
\title{Declare the canonical implementation of a test}
\usage{
baseline(fn, print = NULL)
}
\arguments{
\item{fn}{A function with named arguments. The framework injects
data and arguments by matching formals to the processed model output.}

\item{print}{A function with signature \verb{function(x, ...)} for formatting
the result. \code{NULL} falls back to \code{print(x$data)}.}
}
\value{
A \code{baseline} S7 object.
}
\description{
\code{baseline()} declares the default implementation of a test, which is
the only implementation reachable on the eager path. It is frozen —
no user or package can swap it out via \code{\link[=swap_variant]{swap_variant()}}.
}
\seealso{
\code{\link[=variant]{variant()}}, \code{\link[=agendas]{agendas()}}, \code{\link[=test_define]{test_define()}}
}
