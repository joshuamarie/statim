% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recalibrator-via.R
\name{via}
\alias{via}
\title{Recalibrate the method variant}
\usage{
via(.x, .method, ...)
}
\arguments{
\item{.x}{A \code{test_lazy} or \code{model_lazy} object.}

\item{.method}{A string naming the method variant. Must match a named
\code{\link[=variant]{variant()}} in the \code{\link[=agendas]{agendas()}} of the matched \code{\link[=stat_define]{stat_define()}}.
E.g. \code{"boot"}, \code{"permute"}, \code{"permute_rfast"}.}

\item{...}{Named arguments forwarded to the variant.}
}
\value{
The modified lazy object with \code{recalibrate_spec} populated.
}
\description{
\code{via()} switches a lazy pipeline to an alternative method variant and
merges user-supplied arguments with the variant's declared defaults.
Works for both \code{test_lazy} and \code{model_lazy} pipelines.
}
\examples{
sleep |>
    define_model(x_by(extra, group)) |>
    prepare_test(TTEST) |>
    via("boot", n = 2000) |>
    conclude()

sleep |>
    define_model(x_by(extra, group)) |>
    prepare_test(TTEST) |>
    via("permute", n = 999L) |>
    conclude()

}
\seealso{
\code{\link[=conclude]{conclude()}}, \code{\link[=stat_define]{stat_define()}}
}
