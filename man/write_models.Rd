% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-expand.R
\name{write_models}
\alias{write_models}
\title{Write multiple model definitions from a data frame}
\usage{
write_models(.data, ...)
}
\arguments{
\item{.data}{A data frame.}

\item{...}{Named model expressions. Each must evaluate to a formula or
a \code{model_id} object. Names are used as row labels in \code{\link[=anova]{anova()}}
output.}
}
\value{
An \code{expanded_model} object.
}
\description{
\code{write_models()} evaluates named model expressions sequentially against
\code{.data}, so each name is available to subsequent expressions via
\code{\link[stats:update]{stats::update()}}. Accepts any valid model ID: formulas, \code{\link[=rel]{rel()}},
\code{\link[=x_by]{x_by()}}, or any registered \code{model_id} type.
}
\details{
Sits between a data frame and \code{\link[=prepare_model]{prepare_model()}} or \code{\link[=prepare_test]{prepare_test()}}
in the pipeline.
}
\examples{
# explicit formulas
LifeCycleSavings |>
    write_models(
        f1 = sr ~ 1,
        f2 = sr ~ pop15,
        f3 = sr ~ pop15 + pop75,
        f4 = sr ~ pop15 + pop75 + dpi,
        f5 = sr ~ pop15 + pop75 + dpi + ddpi
    ) |>
    prepare_model(LINEAR_REG) |>
    anova()

# update() chain
LifeCycleSavings |>
    write_models(
        f1 = sr ~ 1,
        f2 = update(f1, ~. + pop15),
        f3 = update(f2, ~. + pop75),
        f4 = update(f3, ~. + dpi),
        f5 = update(f4, ~. + ddpi)
    ) |>
    prepare_model(LINEAR_REG) |>
    anova()

# mixed model_id types
LifeCycleSavings |>
    write_models(
        mod0 = rel(pop15, sr),
        f1 = sr ~ 1,
        f2 = update(f1, ~. + pop15)
    ) |>
    prepare_model(LINEAR_REG) |>
    anova()

}
\seealso{
\code{\link[=prepare_model]{prepare_model()}}, \code{\link[=anova]{anova()}}
}
